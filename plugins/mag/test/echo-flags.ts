/** The `utility echo` fixture's required flags — shared across every test that drives it. */
export const REQUIRED_ECHO_FLAGS = ["--name", "x", "--count", "3", "--raw-field", "y", "--max-retries", "2"] as const
