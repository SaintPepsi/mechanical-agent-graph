/** Dispatch-time facts no compiled variant can know: the run's ticket, and this checkout's skills
 * root. Filled by the caller after composing (`graph-nodes/design/graph-node.ts`'s `skillFor`). */
export const TICKET_TOKEN = "<TICKET>"
export const SKILLS_TOKEN = "<SKILLS>"
