[
 {"id":"eviction-boundary","category":"boundary","file":"src/limiter.ts","find":"const kept = all.filter((t) => t > cutoff)","replace":"const kept = all.filter((t) => t >= cutoff)","describes":"a hit exactly one window old is kept instead of evicted"},
 {"id":"limit-off-by-one","category":"boundary","file":"src/limiter.ts","find":"    if (kept.length >= this.limit) {\n      const oldest = kept[0]!\n      return { allowed: false, remaining: 0, retryAfterMs: oldest + this.windowMs - now }\n    }\n\n    kept.push(now)","replace":"    if (kept.length > this.limit) {\n      const oldest = kept[0]!\n      return { allowed: false, remaining: 0, retryAfterMs: oldest + this.windowMs - now }\n    }\n\n    kept.push(now)","describes":"one more request than the limit is allowed through"},
 {"id":"remaining-off-by-one","category":"arithmetic","file":"src/limiter.ts","find":"    kept.push(now)\n    return { allowed: true, remaining: this.limit - kept.length, retryAfterMs: 0 }","replace":"    kept.push(now)\n    return { allowed: true, remaining: this.limit - kept.length + 1, retryAfterMs: 0 }","describes":"remaining is reported before the hit is counted, so it is always one too high"},
 {"id":"retry-after-newest","category":"arithmetic","file":"src/limiter.ts","find":"    if (kept.length >= this.limit) {\n      const oldest = kept[0]!\n      return { allowed: false, remaining: 0, retryAfterMs: oldest + this.windowMs - now }\n    }\n\n    kept.push(now)","replace":"    if (kept.length >= this.limit) {\n      const oldest = kept[kept.length - 1]!\n      return { allowed: false, remaining: 0, retryAfterMs: oldest + this.windowMs - now }\n    }\n\n    kept.push(now)","describes":"retryAfterMs is measured from the newest hit, so callers wait a whole window too long"},
 {"id":"retry-after-constant","category":"arithmetic","file":"src/limiter.ts","find":"      return { allowed: false, remaining: 0, retryAfterMs: oldest + this.windowMs - now }\n    }\n\n    kept.push(now)","replace":"      return { allowed: false, remaining: 0, retryAfterMs: this.windowMs }\n    }\n\n    kept.push(now)","describes":"retryAfterMs is always the full window rather than the time actually left"},
 {"id":"clock-read-twice","category":"time","file":"src/limiter.ts","find":"    kept.push(now)\n    return { allowed: true, remaining: this.limit - kept.length, retryAfterMs: 0 }","replace":"    kept.push(this.clock())\n    return { allowed: true, remaining: this.limit - kept.length, retryAfterMs: 0 }","describes":"the clock is read a second time to stamp the hit, so eviction and recording can disagree"},
 {"id":"peek-records","category":"purity","file":"src/limiter.ts","find":"    const all = this.hits.get(key) ?? []\n    const kept = all.filter((t) => t > now - this.windowMs)\n\n    if (kept.length >= this.limit) {","replace":"    const all = this.hits.get(key) ?? []\n    const kept = all.filter((t) => t > now - this.windowMs)\n    kept.push(now); this.hits.set(key, kept)\n\n    if (kept.length >= this.limit) {","describes":"peek records a hit, so merely asking consumes quota"},
 {"id":"reset-key-clears-all","category":"contract","file":"src/limiter.ts","find":"    else this.hits.delete(key)","replace":"    else this.hits.clear()","describes":"resetting one key wipes every key"},
 {"id":"reset-all-noop","category":"contract","file":"src/limiter.ts","find":"    if (key === undefined) this.hits.clear()","replace":"    if (key === undefined) return","describes":"reset() with no argument silently does nothing"},
 {"id":"keys-not-isolated","category":"contract","file":"src/limiter.ts","find":"    const all = this.hits.get(key) ?? []\n    const cutoff = now - this.windowMs","replace":"    const all = this.hits.get(\"*\") ?? []\n    const cutoff = now - this.windowMs","describes":"every key shares one bucket, so one caller exhausts everyone's quota"},
 {"id":"eviction-shift-once","category":"boundary","file":"src/limiter.ts","find":"    const kept = all.filter((t) => t > cutoff)\n    this.hits.set(key, kept)","replace":"    const kept = all[0] !== undefined && all[0] <= cutoff ? all.slice(1) : all\n    this.hits.set(key, kept)","describes":"only the single oldest expired hit is evicted per call, not all of them"},
 {"id":"retry-after-when-allowed","category":"contract","file":"src/limiter.ts","find":"    return { allowed: true, remaining: this.limit - kept.length, retryAfterMs: 0 }\n  }\n\n  /** Reports what","replace":"    return { allowed: true, remaining: this.limit - kept.length, retryAfterMs: this.windowMs }\n  }\n\n  /** Reports what","describes":"an allowed request still reports a retryAfterMs, so a well-behaved caller backs off unnecessarily"},
 {"id":"limit-validation","category":"error-path","file":"src/limiter.ts","find":"if (!Number.isInteger(limit) || limit < 1) throw new RangeError","replace":"if (false) throw new RangeError","describes":"a limit of 0 or -1 is accepted and the limiter silently denies or misbehaves"},
 {"id":"peek-boundary-drift","category":"boundary","file":"src/limiter.ts","find":"    const kept = all.filter((t) => t > now - this.windowMs)\n\n    if (kept.length >= this.limit) {","replace":"    const kept = all.filter((t) => t >= now - this.windowMs)\n\n    if (kept.length >= this.limit) {","describes":"peek and check disagree at the window edge, so peek says denied when check would allow"}
]
