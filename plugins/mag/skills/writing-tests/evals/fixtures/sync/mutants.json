[
 {"id":"equal-version-rewritten","category":"boundary","file":"src/sync.ts","find":"existing.version >= item.version","replace":"existing.version > item.version","describes":"a record already at the same version is written again instead of skipped"},
 {"id":"older-version-wins","category":"boundary","file":"src/sync.ts","find":"existing.version >= item.version","replace":"existing.version <= item.version","describes":"a stale remote record overwrites a newer local one"},
 {"id":"retries-twice","category":"error-path","file":"src/sync.ts","find":"    if (!(error instanceof TransientError)) throw error\n    return await remote.page(cursor)","replace":"    if (!(error instanceof TransientError)) throw error\n    try { return await remote.page(cursor) } catch { return await remote.page(cursor) }","describes":"a transient failure is retried twice, not once"},
 {"id":"no-retry","category":"error-path","file":"src/sync.ts","find":"    if (!(error instanceof TransientError)) throw error\n    return await remote.page(cursor)","replace":"    throw error","describes":"a transient failure aborts the sync instead of being retried"},
 {"id":"retries-fatal","category":"error-path","file":"src/sync.ts","find":"    if (!(error instanceof TransientError)) throw error\n    return await remote.page(cursor)","replace":"    return await remote.page(cursor)","describes":"a genuinely fatal error is retried as though it were transient"},
 {"id":"skipped-counted-as-written","category":"contract","file":"src/sync.ts","find":"        skipped += 1\n        continue","replace":"        written += 1\n        continue","describes":"skipped records are reported as writes, so the report overstates what happened"},
 {"id":"fetched-counts-pages","category":"arithmetic","file":"src/sync.ts","find":"    fetched += page.items.length","replace":"    fetched += 1","describes":"fetched counts pages rather than records"},
 {"id":"write-before-check","category":"ordering","file":"src/sync.ts","find":"      const existing = await store.get(item.id)\n      if (existing !== undefined && existing.version >= item.version) {","replace":"      await store.put({ id: item.id, version: item.version, name: item.name })\n      const existing = await store.get(item.id)\n      if (existing !== undefined && existing.version >= item.version) {","describes":"every record is written before the version check, so a skip still clobbers the store"},
 {"id":"put-drops-name","category":"contract","file":"src/sync.ts","find":"await store.put({ id: item.id, version: item.version, name: item.name })","replace":"await store.put({ id: item.id, version: item.version, name: \"\" })","describes":"the name is blanked on the way into the store"},
 {"id":"items-reversed","category":"ordering","file":"src/sync.ts","find":"    for (const item of page.items) {","replace":"    for (const item of [...page.items].reverse()) {","describes":"records within a page are written in reverse order, so two versions of one id land the wrong way round"},
 {"id":"pagination-termination","category":"contract","file":"src/sync.ts","find":"    if (page.nextCursor === null) break","replace":"    if (page.nextCursor === undefined) break","describes":"pagination no longer stops on a null cursor — it runs to the page cap and throws"},
 {"id":"cursor-not-advanced","category":"contract","file":"src/sync.ts","find":"    cursor = page.nextCursor","replace":"    cursor = null","describes":"the cursor is never advanced, so page two is fetched with page one's cursor forever"},
 {"id":"pages-off-by-one","category":"arithmetic","file":"src/sync.ts","find":"    pages += 1\n    fetched","replace":"    pages += 0\n    fetched","describes":"the page count is never incremented"},
 {"id":"missing-record-skipped","category":"boundary","file":"src/sync.ts","find":"      if (existing !== undefined && existing.version >= item.version) {","replace":"      if (existing === undefined || existing.version >= item.version) {","describes":"a record that does not exist locally is skipped instead of created"}
]
