{
  "compilerOptions": {
    "strict": true,
    "moduleResolution": "bundler",
    "module": "ESNext",
    "target": "ESNext",
    "noEmit": true,
    "types": ["bun"],
    "skipLibCheck": true,
    "resolveJsonModule": true
  },
  "include": ["**/*"],
  "//": "projects/** are their own packages: graph-viewer resolves `$lib` and `./$types` through SvelteKit, so its own `check` (svelte-check, run by the root typecheck) is what type-checks it.",
  "exclude": ["skills/**/evals/**", "projects/**"]
}
